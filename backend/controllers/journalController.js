const User = require('../models/Journal'); 
const jwt = require('jsonwebtoken'); 


const journal = async (req, res) => { 
 
}
 
// We'll add more controller functions here later 
module.exports = { 
  journal 
}; 